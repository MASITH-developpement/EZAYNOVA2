# Configuration de l'API de démos Odoo
# Copiez ce fichier en .env et remplissez les valeurs

# URL de votre instance Odoo principale
ODOO_URL=https://ezaynova2-production.up.railway.app

# Master password de l'instance Odoo (même que ADMIN_PASSWORD dans Railway)
MASTER_PASSWORD=admin

# Clé API pour sécuriser l'accès (générez une clé forte)
# Générer avec: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=your-secret-api-key-here

# Chemin pour la base de données SQLite
DB_PATH=demos.db

# Port de l'application (Railway utilise la variable PORT automatiquement)
PORT=8080
